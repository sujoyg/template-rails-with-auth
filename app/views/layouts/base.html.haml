!!!DOCTYPE html
%html
  %head
    %title= content_for?(:title) ? "#{yield(:title)} • #{$globals.application.titlecase}" : $globals.application.titlecase
    = stylesheet_link_tag 'application', :media => 'all'
    = javascript_include_tag 'application'
    = csrf_meta_tags
  %body
    = content_for?(:content) ? yield(:content) : yield